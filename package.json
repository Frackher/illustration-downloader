{
  "name": "img-downloader",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --reporter=verbose"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "piexifjs": "^1.0.6"
  }
}
